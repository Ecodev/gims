<div class="container">

    <h2>
        <ul class="breadcrumb">
            <li><a href="/admin/survey">Surveys</a></li>
            <li class="active">{{survey.code}}</li>
        </ul>
    </h2>

    <form name="myForm">

        <?php echo $this->crudButtons('survey'); ?>

        <tabset style="margin-top: 20px">
            <tab heading="General info" active="tabs[0]" select="selectTab(0)" >
                <div class="control-group" ng-class="{error: myForm.code.$invalid}">
                    <label for="survey.code">Code</label>
                    <input id="survey.code" type="text" name="code" ng-model="survey.code" required />
                    <span ng-show="myForm.code.$error.required" class="help-inline">Required</span>
                </div>

                <div class="control-group" ng-class="{error: myForm.name.$invalid}">
                    <label for="survey.name">Name</label>
                    <input id="survey.name" type="text" name="name" ng-model="survey.name" required class="span6" />
                    <span ng-show="myForm.name.$error.required" class="help-inline">Required</span>
                </div>

                <div class="control-group" ng-class="{error: myForm.isActive.$invalid}">
                    <label for="survey.isActive">Active</label>
                    <select id="survey.isActive" name="active" ng-model="survey.isActive" ng-options="c.value as c.text for c in actives"></select>
                    <span ng-show="myForm.isActive.$error.required" class="help-inline">Required</span>
                </div>

                <div class="control-group" ng-class="{error: myForm.year.$invalid}">
                    <label for="survey.year">Year</label>
                    <input id="survey.year" type="number" name="year" ng-model="survey.year" min="1970" max="2100" required />
                    <span ng-show="myForm.year.$error.number" class="help-inline">Not a valid year</span>
                    <span ng-show="myForm.year.$error.required" class="help-inline">Required</span>
                </div>

                <div class="control-group">
                    <label for="survey.comments">Comments</label>
                    <textarea id="survey.comments" type="text" name="comments" ng-model="survey.comments" class="span6"></textarea>
                </div>

                <div class="control-group input-append">

                    <div class="row-fluid">
                        <div class="span6">
                            <label for="survey.dateStart">Date start</label>
                            <label for="survey.dateStart">
                                <input id="survey.dateStart" class="input-small" type="text" ng-model="survey.dateStart" datepicker-popup="dd/MM/yyyy"/><button type="button" class="btn" data-toggle="datepicker"><i class="icon-calendar"></i></button>
                            </label>
                        </div>
                        <div class="span6">
                            <label for="survey.dateEnd">Date end</label>
                            <label for="survey.dateEnd">
                                <input id="survey.dateEnd" class="input-small" type="text" ng-model="survey.dateEnd" datepicker-popup="dd/MM/yyyy"/><button type="button" class="btn" data-toggle="datepicker"><i class="icon-calendar"></i></button>
                            </label>
                        </div>
                    </div>
                </div>
            </tab>

            <tab heading="Questions" active="tabs[1]" select="selectTab(1)" ui-if="survey.id">
                <div gims-grid-question></div>
            </tab>

            <tab heading="Questionnaires" active="tabs[2]" select="selectTab(2)"  ui-if="survey.id">
                <div gims-grid-questionnaire></div>
            </tab>

            <tab heading="Users" active="tabs[3]" select="selectTab(3)"  ui-if="survey.id">
                <gims-relations relation="UserSurvey" first="survey" second="user" third="role"></gims-relations>
            </tab>
        </tabset>
    </form>

    <?php echo $this->metadata('survey'); ?>
</div> <!-- /container -->
